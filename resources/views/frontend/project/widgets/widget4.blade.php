@if(isset($widget->data->title))
    <div class="project-widget4">
        <div class="container">
            <div class="row">
                <h3 class="project-widget4-title">{{$widget->data->title}}</h3>
            </div>
        </div>
    </div>
@endif
